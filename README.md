# Krishan Kant — Premium Portfolio

Run locally:
```bash
npm install
npm run dev
```

The main content is in `src/main.jsx`; visual styling is in `src/styles.css`.
The hero currently has a premium `Krishan Kant / YOUR PHOTO` placeholder. Replace it with a real profile photo later.
